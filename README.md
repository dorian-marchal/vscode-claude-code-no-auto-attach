A personal VS Code extension that patches the installed Claude Code extension to fix annoyances upstream hasn't addressed yet:

- **Auto-attach defaults to OFF.** Stops Claude Code attaching the current file/selection to every prompt. Addresses [anthropics/claude-code#24726](https://github.com/anthropics/claude-code/issues/24726). Configurable via `claude-code-no-auto-attach.detachContextByDefault` (set to `false` to keep upstream's attached-by-default behavior).
- **Ctrl+F toggles context attachment.** With a Claude session focused, `Ctrl+F` attaches/detaches the current file/selection — independent of the default above.
- **Optional auto-allow for Claude's own files.** In `bypassPermissions` mode, Claude Code still prompts when a tool call touches its own protected paths (e.g. project `.claude/`, `~/.claude/settings.json`). Set `claude-code-no-auto-attach.autoApproveProtectedPathWrites: true` to silently approve `Write`/`Edit`/`MultiEdit`/`NotebookEdit`/`Bash` prompts. Addresses [anthropics/claude-code#37029](https://github.com/anthropics/claude-code/issues/37029).
- **Session-scoped model switching.** Upstream, picking a model in the UI rewrites the global default in `~/.claude/settings.json`, so every future session inherits it. The patch reroutes the switch through the SDK's session-scoped `set_model` control request: switching only affects the current session, new sessions start on your real default.
- **Per-session model badge.** Each Claude session webview shows the current model in a badge (top-right), color-coded per model family (Haiku teal `#269473`, Sonnet gold `#bc8e26`, Opus red `#c63e3e`, Fable purple `#8052d2`; falls back to the default badge color otherwise). When the current model supports effort, the badge also shows the current effort level (e.g. `Opus 4.8 · xhigh`, or `· ultra` under ultracode) and updates live as the effort changes. Click it to open the model picker. Being per-webview, it stays correct with many concurrent sessions. When the last answer came from a different model family than the selected one — a selection that never reached the CLI (e.g. a resumed session, whose `modelSelection` is seeded from the global default) or a refusal fallback — the badge splits in two: the dimmed model that answered, an arrow, then the selected model, each pill colored by its own family. A plain model switch is not drift and stays a single badge.
- **Ctrl+M cycles models.** With a Claude session focused, `Ctrl+M` cycles through the available models for that session only (and sets the effort preset for the model it lands on — see below).
- **Ctrl+0/1/2/3 send with a forced model.** With a Claude session focused, `Ctrl+0` switches the session to Fable, `Ctrl+1` to Opus, `Ctrl+2` to Sonnet, `Ctrl+3` to Haiku — then submits the composer in one keystroke (the keyboard equivalent of the quick-send buttons; Opus has no button of its own). The switch is session-scoped. No-op while the session is busy, when the composer is empty, or when that model isn't available for the session.
- **Sonnet/Haiku/Fable quick-send buttons.** Three extra send buttons sit next to the composer's send button — gold switches the session to Sonnet, teal to Haiku, purple to Fable — then submit the prompt in one click. They reuse the session-scoped `setModel`, so the switch only affects the current session. Each button's tooltip shows the model, the effort it will apply, and its shortcut (e.g. `Send to Sonnet · medium [Ctrl + 2]`). Each button hides when its model isn't available; the original send button keeps its native send/stop behavior.
- **Per-model effort presets.** Every one of the model-switch actions above (the quick-send buttons, `Ctrl+0/1/2/3`, and `Ctrl+M`) also sets the reasoning effort to match the model family: **Fable → `high`**, **Opus → `xhigh`**, **Sonnet → `medium`**, **Haiku → `medium`**. The effort is only changed if the target model actually supports that level (otherwise it's left as-is), and the change is session-scoped like the model switch (via `sessionScopedEffortSwitch`), so it never rewrites your global default.
- 🪲 **Slash commands keep your file context.** Upstream silently drops the current file/selection for *any* message starting with `/` (the flag is computed as `includeSelection && !startsWithSlash`), so invoking a skill or slash command never shows the agent your open file even with the toggle on. The patch makes the toggle authoritative: if context is on (default or `Ctrl+F`), it rides along with slash commands too. Explicitly attached files (`@`-mentions / attach button) were never affected.
- 🪲 **New sessions focus the composer again.** Since 2.1.257 the focus-on-mount effect is gated on `document.hasFocus()`, which a freshly opened Claude tab never satisfies (VS Code's focus lands on the webview shell before the app's iframe exists), so the input stays unfocused. The patch restores the pre-2.1.257 direct focus call, which pulls focus into the webview by itself — exactly the old behavior, including for session switches.
- 🪲 **No usage-limit nagging.** The "You've used 75% of your session limit · resets in 2h" / "Approaching weekly limit" banner fires on every rate-limit event and has to be dismissed by hand each time. The patch suppresses it. The hard "You've hit your <limit>" banner (status `rejected`) is kept — at that point you're actually cut off, so it's worth a click.
- 🪲 **Capped, scrollable prompt bubbles.** A huge pasted prompt (e.g. a long stack trace) no longer fills the whole webview — rendered user prompts are capped at `40vh` and scroll instead. Short prompts are unaffected.
- **`vscode://…/open` URIs open in your locked Claude editor group and auto-send.** Claude Code's (undocumented) URI handler — `vscode://anthropic.claude-code/open?prompt=<text>&session=<id>` — opens the session with `claude-vscode.primaryEditor.open`, i.e. in the **active editor group**, so following a link hijacks whatever group you were working in. The patch routes it through `claude-vscode.editor.open` (the command behind "open Claude in an editor") with the target column resolved by the patch, and locks the group afterwards, so the session lands in the editor group you keep Claude in. A prompt is also **submitted right away** once the session is idle, instead of just being typed (gives up after 30s and leaves the prompt typed). Configurable via `uriOpensInEditor` and `uriAutoSendsPrompt`; turning the first off restores upstream's active-group behavior.
- 🪲 **Markdown previews are picked up as context.** A markdown *preview* isn't a text editor, so focusing it left Claude Code with no current file — you had to switch back to the raw `.md`. The patch handles both of VS Code's preview implementations: the custom-editor preview (`vscode.markdown.preview.editor` / `vscode.markdown.editor`, e.g. when `*.md` is associated with it) exposes its source `.uri` directly, so it's read straight off the tab; the classic "Open Preview" webview doesn't, so its source is resolved from the tab label (preferring the last active markdown editor, then a uniquely-matching open document, then a unique workspace file). Either way the resolved file is attached as the current-file context.

## How it works

On activation, the extension iterates every installed `~/.vscode/extensions/anthropic.claude-code-*` directory and applies independent sub-patches to three files (a sub-patch whose anchor no longer matches is skipped and logged without blocking the others):

- **`webview/index.js`**
  - finds the unique site wiring the attach state to the toggle (`includeSelection:X,onToggleIncludeSelection:()=>Y(`) and, when `detachContextByDefault` is on, flips its `useState(!0)` to `useState(!1)`. The `[X,Y]` pair is reused by an earlier component, so the patch anchors on the declaration closest *before* the toggle prop (same component) rather than the first match in the file;
  - rewrites the same toggle prop into an IIFE that exposes the toggle as `globalThis.__ccaaToggleContext` and binds a capture-phase `Ctrl+F` keydown calling it (the original click toggle is preserved and reconstructed byte-exact on revert);
  - finds the submit handler's include-selection computation (`let gt=v&&!De`, where `v` is the toggle and `De` is "message starts with `/`") and drops the `&&!De` guard so slash commands and skills keep the file/selection when the toggle is on. The stripped `&&!De` is parked in a `/*__ccaaSlashSel:…*/` sentinel comment for a byte-exact revert;
  - appends to the reactive effect that registers the "Switch model…" command action: a DOM badge kept in sync with the session's `modelSelection` (and its `effortLevel`/`currentModelSupportsEffort`/`ultracodeEnabled` signals, so it shows `Model · effort` when the model supports effort and re-renders on effort changes). The badge is a flex row of three spans (`#ccaa-model-badge-served`, `-arrow`, `-main`); the first two show only on family drift, computed from the model family of `modelSelection` vs. `lastServedModel` only — upstream clears `lastServedModel` inside `setModel`, so a switch shows no drift until an answer disagrees, whereas `currentMainLoopModel` keeps the pre-switch value and made every switch look like drift; it is used solely as a color hint when the selection has no family (`default`). Each pill takes its name from its own model (never from upstream's label, which names the selected or the served model depending on upstream's own drift rule) and its color from that model's family; two effort helpers (`__ccaaEffortFor(model)` maps a model family to its preset effort — Fable→`high`, Opus→`xhigh`, Sonnet/Haiku→`medium` — when the model's `supportsEffort`/`supportedEffortLevels` allow it, else null; `__ccaaApplyEffort(session,model)` calls the session's `setEffortLevel`); two tooltip helpers (`__ccaaShowTip(el,text)`/`__ccaaHideTip()` position a single reused `#ccaa-tip` node above a hovered element — instant, unlike the native `title` delay — used by the quick-send buttons); plus a capture-phase keydown listener — `Ctrl+M` cycles `setModel` to the next available model (then applies its preset effort), and `Ctrl+0`/`Ctrl+1`/`Ctrl+2`/`Ctrl+3` switch to the first Fable/Opus/Sonnet/Haiku model (matched by `value`/`displayName`), apply its preset effort, and then `requestSubmit()` the composer. The switch is skipped only when `__ccaaNeedsSwitch(session,model,re)` says the session already runs that family: `modelSelection` alone is not enough, since it is seeded from the *global* default model setting on launch — which the session-scoped `setModel` patch never writes — so a resumed session reads e.g. `opus[1m]` while the CLI still serves Fable, and the send would keep the old model yet apply the target's effort. `lastServedModel`/`currentMainLoopModel` are the drift check (found via the `button[type="submit"][data-permission-mode]` send button, skipped while busy or when its send button is disabled);
  - finds the unique rate-limit banner construction in the `rate_limit_event` handler and gates it on `info.status==="rejected"`, so only the hard limit-reached banner survives; the original call is parked inside a `/*__ccaaRateLimit*/…*/` sentinel for a byte-exact revert. Three bundle generations are matched: 2.1.251+ routes the event through a `"clear"/"show"/"keep-hidden"` decision helper and builds the banner in the `"show"` branch — assigned directly (`let Q=zQ0(Y);this.rateLimitWarning.value=Q,this.shownRateLimitKey=…`) in 2.1.251, and since 2.1.257 to `frameRateLimitWarning`/`shownRateLimit` inside a signal batch (`let Q=uZ0(Y);$H(()=>{this.frameRateLimitWarning.value=Q,…})`); older bundles assigned it directly behind a `dismissedRateLimitKey` check;
  - registers (editor panels only, at the app bootstrap where the session store is in scope) a window `message` listener for the custom `ccaa-open` message posted by the extension.js uri-open patch. The extension has already opened the panel on the right session, so the listener only finishes the job: it types the prompt via the stock `initialPrompt` signal when the extension could not hand it to the stock command (`typePrompt`, set when the session already had a panel), and — when `autoSend` is set — polls until the prompt has reached the composer, the session is idle and the send button is enabled, then `requestSubmit()`s the composer (30s timeout; also aborts if the session is switched). The message is ignored until the panel actually shows the URI's session (`match`), which is what keeps a broadcast landing in a single panel; `strict` (set only for that broadcast) gives the match 5s before the panel rules itself out. A nonce set dedupes the extension's delivery retries, the side bar ignores the message (`window.IS_SIDEBAR` gate), and the stock webview listener never sees this type at all.
  - finds the unique session-mount focus effect (`if(nk(document.activeElement))return;jV1(()=>q.current?.focus(),()=>J.ambientFocusAllowed())`, keyed on `sessionId`) and replaces the gated `jV1(...)` wrapper with the bare `q.current?.focus()` it wraps — the composer's stock focus handle, which still goes through `safeFocus`. The wrapper is parked in a `/*__ccaaMountFocus:…*/` sentinel comment for a byte-exact revert;
  - finds the unique composer send button (`type:"submit"` + `className:X.sendButton` + `data-permission-mode`) and inserts three `type:"button"` siblings after it (Sonnet/gold, Haiku/teal, Fable/purple) that call the in-scope session's `setModel` for the matching model (skipped via `__ccaaNeedsSwitch`, falling back to a plain `modelSelection` comparison if the badge patch skipped), apply its preset effort, then `requestSubmit()` the form. Each carries an `aria-label` and an instant custom hover tooltip (via `__ccaaShowTip`/`__ccaaHideTip`) reading e.g. `Send to Sonnet · medium [Ctrl + 2]`.
- **`webview/index.css`**
  - appends a rule capping the user prompt bubble at `max-height:40vh` with `overflow-y:auto`. It targets the bubble via a hash-independent attribute selector (`[class*="userMessage_"]`) and is appended last so its equal-specificity `overflow-y` wins over upstream's `overflow-y:hidden`.
- **`extension.js`**
  - finds the unique `can_use_tool` site and injects an early return that approves Write/Edit/MultiEdit/NotebookEdit/Bash when `autoApproveProtectedPathWrites` is on;
  - captures permission-mode changes into `globalThis.__ccaaPermissionMode` to gate the above;
  - replaces the body of the `set_model` handler — upstream's `writeUserSettingsAndPush({model})` (which persists to `~/.claude/settings.json`) — with `withChannel(...query.setModel(...))`, the session-scoped control request, when `sessionScopedModelSwitch` is on;
  - makes effort switching session-scoped when `sessionScopedEffortSwitch` is on: in the `apply_settings` handler, any effort-only settings object (single key `effortLevel`, sent both by the native effort picker and by our model buttons/shortcuts) is forced to `flagsOnly`, so `writeUserSettingsAndPush` skips the `~/.claude/settings.json` write but still pushes the effort to the running session via `applyFlagSettings`. Since 2.1.257 `applySettings` takes a 4th `scope` argument (`"localSettings"` writes the project's `settings.local.json` and rejects `flagsOnly`), so the override only fires when no scope is passed — the effort picker never passes one;
  - rewrites the URI handler's `/open` case (anchored across the `/install-plugin` tail, whose `notifyOpenPluginsDialog` call is the only in-scope reference to the webview manager; 2.1.257's session-id validation guard is matched optionally and kept ahead of the injected code): when `uriOpensInEditor` is on, it runs `claude-vscode.editor.open` instead of `primaryEditor.open` (which targets `ViewColumn.Active`), passing the Claude group's column and locking that group afterwards, then posts a custom top-level `{type:"ccaa-open",prompt,match,typePrompt,strict,autoSend,nonce}` message to the panel it just opened, retrying every 500ms for ~15s because the panel may still be booting.
    - *Which group.* Stock's own rule — a group whose tabs are **all** `claudeVSCodePanel` webviews — loses the group the moment a file is dropped into it, and then banishes the session to a fresh column. The patch instead takes the group holding the most Claude sessions (ties → rightmost) whatever else sits in it, and failing that an open-but-empty group: the API exposes no lock state (`TabGroup` has no `isLocked`), but VS Code only keeps an empty group around when it is locked, which is exactly the empty Claude group waiting for a session. With neither, the column is left undefined and stock creates + locks one.
    - *Side effect.* Since 2.1.257 `claude-vscode.editor.open` also records `claudeCode.preferredLocation = "panel"` whenever it is called with a non-active column, exactly as when you open Claude in an editor by hand — so a URI opened this way also makes the editor the preferred location.
    - *The lock.* Stock only locks a group it had to create, so reusing a group that got unlocked would leave it unlocked. The panel takes focus (`createWebviewPanel` gets a bare `ViewColumn`), so the group it landed in is the active one: if it holds a Claude session and is not the last remaining group — locking that one traps the editor — `workbench.action.lockEditorGroup` runs, which is a no-op when it already is locked.
    - *Which webview.* The target is found by diffing the manager's `webviews` set around the open call, so the message is pinned to one panel. When the session already had a panel nothing is added — it is only revealed, and the stock command drops the prompt ("Session is already open…") — so the prompt is withheld from the command (no warning), `typePrompt` is set and the message is broadcast to the chat webviews with `strict`, the session id in `match` picking the one panel showing it. Un-patched webviews ignore the unknown message type (the stock listener only handles `from-extension`), so a partial patch degrades to a no-op. The original `primaryEditor.open` call is kept as the fallback when the setting is off or anything throws;
  - in the `onDidChangeActiveTextEditor` handler, when `activeTextEditor` goes undefined and the active tab is a markdown preview, resolves the source `.md` and pushes it as the current-file context (the same `{filePath,startLine,endLine}` shape upstream produces for an unselected file) instead of clearing it. Two preview kinds are covered: a custom-editor preview (`TabInputCustom`, viewType `vscode.markdown.*`) exposes `.uri` so the path is read directly; the classic webview preview (`TabInputWebview`, viewType `markdown.preview`) has no uri, so the source is resolved from the tab label's basename — preferring the last active markdown editor, then a unique open markdown document, then a unique `findFiles` match. Two more insertions: one at the top of the handler records the last active markdown editor, and one registers (once) `window.tabGroups.onDidChangeTabs` + `onDidChangeTabGroups` listeners that re-run the same resolver on active-tab changes — `onDidChangeActiveTextEditor` only fires on text-editor changes, and switching between two preview tabs in a group flips their `isActive` (an `onDidChangeTabs` event) rather than changing the active group, so without these listeners that switch wouldn't update the context. A filePath dedup keeps the paths from double-firing. All three revert by stripping their sentinel blocks. (Opt-in debug: `touch ~/.ccaa-debug` then reload — each event logs the active tab's input type / viewType / uri / label to `<tmpdir>/ccaa-md-debug.log`.)

Each patched file is prefixed with a versioned marker so re-launches don't re-patch. When the patch logic changes the marker version is bumped, and `applyPatch` reverts any older marker before re-applying so the rollover is seamless. `vscode.extensions.onDidChange` triggers a re-apply whenever Claude Code updates.

## Settings

- `claude-code-no-auto-attach.detachContextByDefault` (boolean, default `true`) — start new Claude sessions with the current file/selection **not** attached as context. Set to `false` to keep upstream's attached-by-default behavior. Either way, `Ctrl+F` toggles the attachment while a Claude session is focused. Because this is baked in at patch time (the webview can't read VS Code settings), changing it re-applies the patch and prompts a window reload.
- `claude-code-no-auto-attach.autoApproveProtectedPathWrites` (boolean, default `false`) — auto-approve `Write`/`Edit`/`MultiEdit`/`NotebookEdit`/`Bash` prompts that fire against Claude's own protected paths despite `bypassPermissions`. The setting is mode-gated, but the gate only takes effect after permission mode is set/changed once in the session — toggle to `bypassPermissions` once at session start to arm it. The injected code reads the setting on every prompt, so toggling takes effect immediately — no reload needed. ⚠️ Only enable in trusted workspaces.
- `claude-code-no-auto-attach.sessionScopedModelSwitch` (boolean, default `true`) — make model switching session-scoped instead of rewriting the global default. Read on every switch, so toggling takes effect immediately. To change your **global default** model: toggle this OFF, switch the model in any session, toggle back ON — or edit the `model` key in `~/.claude/settings.json` directly.
- `claude-code-no-auto-attach.uriOpensInEditor` (boolean, default `true`) — make `vscode://anthropic.claude-code/open?prompt=…&session=…` URIs open/resume in the locked editor group you keep Claude Code in (creating and locking a side column when there is none) instead of hijacking the active editor group. Read on every URI, so toggling takes effect immediately.
- `claude-code-no-auto-attach.uriAutoSendsPrompt` (boolean, default `true`) — when such a URI carries a prompt (and `uriOpensInEditor` is on), submit it right away instead of just typing it into the composer. Waits for the session to be idle; gives up after 30s and leaves the prompt typed.
- `claude-code-no-auto-attach.sessionScopedEffortSwitch` (boolean, default `true`) — make effort switching session-scoped instead of rewriting the global default. Covers both the native effort picker and the per-model effort presets applied by the model buttons/shortcuts. Read on every switch, so toggling takes effect immediately. To change your **global default** effort: toggle this OFF, change the effort in any session, toggle back ON — or edit the `effortLevel` key in `~/.claude/settings.json` directly.

## Caveats

- ⚠️ This modifies files inside another extension's install directory. Expect it to break if Anthropic significantly refactors their bundle. When that happens the extension logs the reason to the `Claude Code: No Auto-Attach` output channel and skips only the broken sub-patch, nothing destructive.
- ⚠️ VS Code doesn't currently verify extension integrity post-install, but if that ever changes this approach could stop working.
- ⚠️ `autoApproveProtectedPathWrites` bypasses Claude Code's protected-path safety check (covering `.claude/`, `~/.claude/settings.json`, etc.). Don't enable it in workspaces where you don't fully trust the agent.
- The model badge, `Ctrl+M`, `Ctrl+0/1/2/3`, and `Ctrl+F` handlers live inside the Claude webview, so the shortcuts only fire when a Claude session has focus (that's also what makes them target the right session). They're captured there (capture-phase + `preventDefault`), so while the session is focused `Ctrl+F` won't reach VS Code's find and `Ctrl+0/1/2/3` won't reach VS Code's focus-editor-group.
- The markdown-preview resolver re-runs both when focus moves from a text editor to a preview and when you switch directly between preview tabs (it also listens to active-tab changes, not just active-text-editor changes). Two identically-named `.md` files that are both closed can't be disambiguated by the tab label — in that case it falls back to upstream's clear/retain behavior.
- A window reload is required after the patch is applied; the extension will prompt you.

## Install

```
./install
```

(packages the vsix with `vsce` and runs `code --install-extension`)

## Commands

- **Claude Code No Auto-Attach: Reapply Patch** — manually re-run the patch (useful after a Claude Code reinstall).
- **Claude Code No Auto-Attach: Revert Patch** — strips the marker and reverses all patches across every installed Claude Code version. The extension will re-apply on next startup unless you disable it first.
